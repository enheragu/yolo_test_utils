# Config package - lightweight configuration without heavy dependencies
# Paths are general config, used by multiple modules
from .paths import yolo_output_path, yolo_output_path_2, yolo_output_log_path, repo_path, home
